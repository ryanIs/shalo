package bin.combat_statistics
{
	/*
		Combat statistics container class for the user (hero)
	*/
	public class HeroBattleStatistic extends BattleStatistic
	{
		public function HeroBattleStatistic(classType:Number)
		{
			super(classType);
		}
	}
}